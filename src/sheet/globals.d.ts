/** Stamped in by Vite at build time. See src/stamp.ts. */
declare const __BUILD_STAMP__: string;

/** Vite turns a CSS import into a side effect that injects the stylesheet. */
declare module "*.css";
